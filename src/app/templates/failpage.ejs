<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Verification Error</title>
    <style>
      /* Reset some default browser styles */
      body,
      h1,
      p,
      button {
        margin: 0;
        padding: 0;
      }

      /* Center the content vertically and horizontally */
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f0f0f0; /* Background color for the entire page */
      }

      .container {
        text-align: center;
        max-width: 500px;
      }

      .error-message {
        background-color: #fff; /* Background color for the error message */
        padding: 40px;
        border-radius: 5px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2); /* Add a subtle shadow to the error message */
      }

      h1 {
        font-size: 32px;
        color: #ff5252; /* Red color for emphasis */
        margin-bottom: 20px;
      }

      p {
        font-size: 20px;
        color: #777;
        margin-bottom: 20px;
      }

      a {
        background-color: #007bff;
        color: #fff;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease-in-out; /* Smooth background color transition */
        text-decoration: none;
      }

      a:hover {
        background-color: #0056b3;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="error-message">
        <h1>Oops! Something Went Wrong</h1>
        <p class="para"><%= message %></p>
        <p>
          <%= text %>
          <a id="regenerate-btn" href="<%= link %>"><%=linktext %></a>.
        </p>
      </div>
    </div>
    <script src="script.js"></script>
  </body>
</html>
