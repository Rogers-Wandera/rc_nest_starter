<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Account Verification Successful</title>
    <style>
      /* Reset some default browser styles */
      body,
      h1,
      p,
      a {
        margin: 0;
        padding: 0;
      }

      /* Center the content vertically and horizontally */
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f0f0f0; /* Background color for the entire page */
      }

      .container {
        text-align: center;
        background-color: #fff; /* Background color for the container */
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2); /* Add a subtle shadow to the container */
        animation: fadeIn 0.5s ease-in-out; /* Fade-in animation */
      }

      .success-message {
        margin: 20px;
        padding: 20px;
        border: 1px solid #ccc; /* Add a border to the success message */
        border-radius: 5px;
        transform: scale(0.9); /* Initial scale for the message */
        animation: bounceIn 0.7s ease-in-out forwards; /* Bounce-in animation */
      }

      h1 {
        font-size: 24px;
        color: #333;
        margin-bottom: 10px;
      }

      p {
        font-size: 18px;
        color: #555;
        margin-bottom: 10px;
      }

      a {
        text-decoration: none;
        color: #007bff;
        font-weight: bold;
        transition: color 0.3s ease-in-out; /* Smooth color transition */
      }

      /* Hover effect for the link */
      a:hover {
        color: #0056b3;
      }

      /* Keyframe animations */
      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }

      @keyframes bounceIn {
        from {
          transform: scale(0.9);
          opacity: 0;
        }
        to {
          transform: scale(1);
          opacity: 1;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="success-message">
        <h1>Account Verification Successful</h1>
        <p><%= message %></p>
        <p>
          <%= text %>
          <a href="<%=link%>" onclick="localStorage.clear()"><%= linktext %></a>
          to your account.
        </p>
      </div>
    </div>
  </body>
</html>
